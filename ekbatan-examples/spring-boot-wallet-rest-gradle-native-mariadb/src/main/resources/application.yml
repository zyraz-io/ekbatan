spring:
  application:
    name: spring-boot-wallet-rest-gradle-native-mariadb


# Single source of truth for DB connection details — both Flyway (run on startup via
# FlywayConfiguration) and Ekbatan's runtime pools read from this same block. Spring Boot's
# own DataSource/Flyway auto-config is intentionally NOT used.
#
# Notes on the JDBC URLs:
#   - The main DB is `wallet`; eventlog is a separate database `eventlog`, created by the V0000
#     Flyway migration. The connecting user needs cross-database privileges — set up by
#     `mariadb_init.sql` mounted into /docker-entrypoint-initdb.d. See docs/database/mariadb.md.
ekbatan:
  namespace: example.wallet.gradle-native-mariadb
  sharding:
    defaultShard:
      group: 0
      member: 0
    groups:
      - group: 0
        name: default
        members:
          - member: 0
            name: default
            configs:
              primaryConfig:
                jdbcUrl: jdbc:mariadb://localhost:3306/wallet
                username: wallet
                password: wallet
                driverClassName: org.mariadb.jdbc.Driver
                maximumPoolSize: 20
              jobsConfig:
                jdbcUrl: jdbc:mariadb://localhost:3306/wallet
                username: wallet
                password: wallet
                driverClassName: org.mariadb.jdbc.Driver
                maximumPoolSize: 5
  local-event-handler:
    handling:
      enabled: true

logging:
  level:
    io.ekbatan: INFO
    io.example.wallet: DEBUG

