# The version of Ekbatan published on Maven Central.
ekbatanVersion=0.1.2

# Daemon heap — generous to keep the JOOQ Docker codegen and Spring Boot tests happy.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
