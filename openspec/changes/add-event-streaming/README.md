# add-event-streaming

Refactor event persistence to a denormalized single-table strategy, add namespace support, introduce broker-agnostic event handler contract, and demonstrate full Kafka streaming pipeline in integration tests
